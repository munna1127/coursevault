<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Debug builds also need internet access so Flutter can talk to the tool. -->
    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
